window.addEventListener('load',()=>{document.getElementById('loader').classList.add('hide')});
const menuBtn=document.getElementById('menuBtn'),menu=document.getElementById('menu');
menuBtn.addEventListener('click',()=>menu.classList.toggle('open'));
document.querySelectorAll('.menu a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
const reveals=document.querySelectorAll('.reveal');function revealOnScroll(){reveals.forEach(el=>{if(el.getBoundingClientRect().top<innerHeight-80)el.classList.add('active')})}addEventListener('scroll',revealOnScroll);addEventListener('load',revealOnScroll);
const slides=document.querySelectorAll('.slide'),dots=document.querySelectorAll('.dot');let current=0;function showSlide(i){slides[current].classList.remove('active');dots[current].classList.remove('active');current=i;slides[current].classList.add('active');dots[current].classList.add('active')}dots.forEach(d=>d.addEventListener('click',()=>showSlide(Number(d.dataset.slide))));setInterval(()=>showSlide((current+1)%slides.length),4500);
const filterButtons=document.querySelectorAll('.filter-btn'),demoCards=document.querySelectorAll('.project');filterButtons.forEach(btn=>btn.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;demoCards.forEach(c=>c.classList.toggle('hide',!(f==='all'||c.dataset.category===f)))}));
function sendWhatsApp(e){e.preventDefault();const phone='216XXXXXXXX';const name=document.getElementById('name').value;const service=document.getElementById('service').value;const message=document.getElementById('message').value;const text=`Bonjour Formationprot,%0AJe m'appelle ${name}.%0AService demandé: ${service}.%0AMessage: ${message}`;window.open(`https://wa.me/${phone}?text=${text}`,'_blank')}
